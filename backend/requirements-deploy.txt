# Slim dependency set for free-tier deployment (Render / Fly).
# Everything from requirements.txt EXCEPT sentence-transformers/torch,
# which is replaced by fastembed (ONNX runtime - ~10x smaller, fits in 512MB RAM).
# The embedder auto-detects which backend is installed, so no code change is needed.

fastapi==0.115.0
uvicorn[standard]==0.30.6

pydantic==2.9.2
pydantic-settings==2.5.2

SQLAlchemy==2.0.35

vaderSentiment==3.3.2

faiss-cpu==1.8.0
numpy==1.26.4
fastembed==0.3.6

httpx==0.27.2

pytest==8.3.3
