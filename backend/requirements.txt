# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Config + validation
pydantic==2.9.2
pydantic-settings==2.5.2

# Database
SQLAlchemy==2.0.35

# Part 1 - sentiment
vaderSentiment==3.3.2

# Part 2 - embeddings + vector search
faiss-cpu==1.8.0
numpy==1.26.4
# Local embedding backend (heavier, pulls in torch).
# On a small free tier this can be swapped for fastembed - see requirements-deploy.txt
sentence-transformers==3.1.1

# Calling the LLM (OpenRouter is OpenAI-compatible, plain HTTP is enough)
httpx==0.27.2

# Tests
pytest==8.3.3
